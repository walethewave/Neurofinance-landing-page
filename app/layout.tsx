import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "NeuroFinance - AI-Powered Financial Intelligence",
  description: "Transform your financial decisions with cutting-edge AI technology. NeuroFinance provides intelligent insights, predictive analytics, and automated portfolio management.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
